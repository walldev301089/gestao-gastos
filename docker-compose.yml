services:
  db:
    image: mysql:8.0
    container_name: mysql_gestao_gastos
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: gestao_gastos_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: